import { ReservationsFilterPipe } from './reservations-filter.pipe';

describe('ReservationsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ReservationsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
