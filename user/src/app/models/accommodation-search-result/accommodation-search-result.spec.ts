import { AccommodationSearchResult } from './accommodation-search-result';

describe('AccommodationSearchResult', () => {
  it('should create an instance', () => {
    expect(new AccommodationSearchResult()).toBeTruthy();
  });
});
